- name: Setup HostName
  ansible.builtin.shell: sudo set-hostname {{ component}}

- name: Install Exporter
  ansible.builtin.import_role:
    name: prometheus
    tasks_from: exporter
  when: node_exporter